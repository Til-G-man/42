#include <stdlib.h>
#include <stdint.h>
#include <stdio.h>
#include "ft_printf.h"

int	main(void)
{
	char	*mem;
	char	*mem2;
	mem = calloc(SIZE_MAX, SIZE_MAX);
	printf("%s", mem);
	mem2 = ft_calloc
}
